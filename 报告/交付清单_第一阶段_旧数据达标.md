# 超声指纹识别 — 第一阶段 交付清单（旧数据·精度达标）

**日期**：2026-07-01
**范围**：老数据（无贴屏 + 不贴屏，27 类手指）上的**指纹匹配精度达标**——纯纹理方向（禁 SIFT）。
**说明**：本包仅含第一阶段（旧数据）内容；贴屏/新数据实验、部署空间时间优化均不在此包。

---

## 一、达标结论（一页看懂）

**目标**：FAR < 0.002%（1/5万）时 FRR < 3%。

| 范围 | EER | FAR=0 时 FRR | 结论 |
|------|-----|------------|------|
| **全 27 根手指** | 1.83% | **2.34%** | ✅ **达标**（<3%） |
| 排除 1 根采集退化手指（wtp_xyz_R1） | 1.00% | **0.64%** | ✅ 优于参考方案 0.96% |

- **冒充被彻底压制**：冒充的几何一致内点 max ≈ 19，FAR=0 完全可达。
- **零 SIFT**：纯自训纹理描述子。
- 2 根手指（btp_fys_R0 / wtp_xyz_R1）的 test 段为**采集退化**（注册段匹配良好、test 帧内点≈0），SIFT / CNN / 纹理三方交叉确认是**数据损坏**，卡全集下限；25 根干净手指达标充分。

## 二、方法（纹理法，三步）

1. **局部纹理描述子**（HardNet 风格自训）：32×32 脊线 patch → 128 维 L2 描述子；**真实跨帧对应点**重训（同指两帧对齐的 RANSAC 内点对作正样本，比纯增广更贴近真实变化），可 bootstrap。
2. **几何验证 = 姿态对齐**：密集网格（stride 6）提描述子 → 互最近邻 + ratio 匹配 → RANSAC 估位姿 → 变换约束（scale∈[0.85,1.18]、|rot|<30°）压制冒充 → 几何一致内点数为匹配分。
3. **重叠面积归一化**：按对齐后前景重叠面积归一化内点数，使不同重叠的分数可比。

**其它路线（同为老数据）**：两级全局+局部（FAR=0 时 FRR 2.1%/25 根）；CNN 全局嵌入 V21+V18b 融合（单场景 EER 1.10%，但连续分数尾部重叠、达不到严格 FAR——严格 FAR 需内点几何的离散可分分布）。

---

## 三、报告文档（`报告/`）

| 文档 | 内容 |
|------|------|
| `客户报告_指纹匹配达标实现` | **主报告（客户版）**：达标结论 + 方法 + 实现 |
| `实验汇报A_精度达标` | 达标详细：全 27 根 FAR=0 FRR 2.34% / 25 根 0.64% |
| `纹理方法_实现交付` | 纹理法（自训描述子 + 姿态对齐）实现说明 |
| `内点匹配器_实现交付` | 局部描述子 + 几何内点验证 实现说明 |
| `内点匹配_复现压测审阅` | 参考方案复现与压测审阅 |
| `JIPNet试水结论` | JIPNet 预训练试水（单场景 EER 15.5%，域差不具竞争力） |

## 四、代码（`代码/`）

**本包含第一阶段（旧数据）全部代码版本**：`代码/推理API/`（可直接调用）+ `代码/全部版本_旧数据/`（**67 个脚本**，v2→v23 全部实验、达标与未达标一并保留）。

### 推理 API（`代码/推理API/`，可直接调用）
| 文件 | 说明 |
|------|------|
| `predict.py` | 自包含推理接口 `FingerprintMatcher`：`feat/match_score/enroll/verify` |
| `best.pth` | 交付纹理描述子（texdesc_v3，128 维，1.23MB） |
| `README.md` | 调用说明 |

### 全部版本（`代码/全部版本_旧数据/`，67 个脚本）
下面按类别标注，**其中达标核心已加粗**；其余为探索/对照/否定性验证版本（全部保留）。

**达标核心（纹理法）**：**`train_descriptor.py`**、**`mine_and_train.py`**（真实对应点挖掘+bootstrap 重训）、**`texture_matcher.py`**（密集网格+姿态对齐+重叠归一化）；`train_descriptor_kp.py`/`texture_matcher_kp.py`（SIFT 关键点变体）。

**达标核心（两级/内点）**：**`combined_two_stage.py`**（全局预筛+局部几何验证）；`improved_matcher.py`/`final_check.py`/`final_matcher.py`/`final_matcher2.py`/`final_confirm.py`（变换约束几何内点匹配器）；`learned_desc_matcher.py`/`learned_local_align.py`（学习局部描述子+对齐）。

**CNN 与融合（v11–v23）**：`optimize_v11_enhanced_cnn` … `optimize_v23_gabor_enhance`、`optimize_v21_hybrid`（V21）、`optimize_v21r34`、`optimize_v18b_local`（V18b）、`optimize_v15/v20_delf`、`fusion_ensemble/final/iter2-4`、`global_local_eval`、`train_arcface`。

**早期方法（v2–v9）**：`optimize_v2`/`v2_fast`/`v3_siamese`/`v4_attention`/`v5_fix`/`v5_simclr_30class`/`v6_multireg_simclr`/`v7_final`/`v8_enhanced`/`v9_single_frame`。

**SIFT 与可行性验证**：`sift_no_xzc`/`sift_no_xzc_fast`/`sift_pipeline_v10`/`sift_pose_feasibility`、`feasibility_orientation`/`feasibility_ridge`（方向场/脊线证伪）、`analyze_btp_perfinger`。

**评估 / 出图 / 工具**：`metrics_27`、`reproduce_stress`、`plot_results`、`replot`/`replot_tex`/`replot_twostage`/`replot_v2`、`generate_report`/`generate_report_v14`/`generate_comparison_chart`、`make_html_report`、`make_visual_compare`、`corrupted_frames_montage`、`read_pptx`。

> **不含**第二阶段（新数据/贴屏）代码：贴屏精度、部署空间/时间优化、V24/V25/V26、top1000、CPU 耗时等脚本均已排除。

## 五、模型（`模型/`）
- `models_texdesc/`：纹理描述子模型（texdesc / v2 / v3 / v4 / kp）。**best.pth = texdesc_v3**（已在推理 API 内）。
- 说明：CNN 模型（models_v21 / v18b 各 130MB+）体积大，未随包附带；如需 CNN 方法可另取。

## 六、图（`图/`）
| 图 | 内容 |
|----|------|
| `匹配效果_纹理方法.png` | 纹理法匹配效果（达标） |
| `匹配效果_v2达标.png` | FAR=0 @ FRR 2.34% 匹配分布 |
| `匹配效果_两级全局加局部.png` | 两级法匹配效果 |
| `全局加局部_严格工作点.png` | CNN 融合在严格 FAR 下尾部重叠（说明为何需内点几何） |
| `损坏帧核查_2根手指.png` | 2 根采集退化手指的 注册帧 vs test 帧对比 |
| `comparison_all_methods.png` / `ffr_vs_far_curve.png` / `summary_table.png` | 各法对比 / FRR-FAR 曲线 / 汇总表 |

## 七、运行环境
- Python：`E:\ANACONDA_NEW\python.exe`（torch 2.11+cu128，RTX 5080）
- 前置：环境变量 `KMP_DUPLICATE_LIB_OK=TRUE`；中文路径读图用 `cv2.imdecode`、写图用 `cv2.imencode`。
